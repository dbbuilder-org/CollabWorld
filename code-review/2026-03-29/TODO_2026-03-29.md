# TODO — 2026-03-29

## Completed This Session (v3 Remediation)
- [x] Fixed 6 failing tests across 3 test files
- [x] 46/46 test files passing, 410/410 tests green
- [x] Removed 3 `console.log` statements from production paths
- [x] Resolved 1 TODO (welcome email → implemented)
- [x] Wired Stripe webhook end-to-end with DB writes
- [x] Added User subscription schema fields + regenerated Prisma client
- [x] `force-dynamic` on all 20 DB-querying pages
- [x] Loading/error UI for dashboard, contests, watch routes
- [x] Sitemap extended with /feed + watch entries
- [x] Contest detail SEO metadata
- [x] Accessibility: aria-label on engagement buttons
- [x] `packages/db/.env` created for Prisma CLI

## Sprint 1 (Next)

### Critical
- [ ] `prisma db push` on staging/prod after next deploy (new User fields)
- [ ] Clerk auth mock helper to fix ~30 test-file TS errors

### High
- [ ] `/contests/[slug]/enter` page — currently a broken CTA link
- [ ] `/profile` page — BottomNav links there but page doesn't exist
- [ ] Premium UI gating (subscriptionPlan is now on User, needs UI surface)

### Medium
- [ ] Avatar upload → R2 (local disk storage breaks on Render)
- [ ] Stripe webhook tests for subscription.updated + subscription.deleted
- [ ] Structured logging service (replace console.error)

## Known Issues
- [ ] `__tests__/sprint7/*` have ~30 TS type errors in test files (tests pass, TS errors only)
- [ ] `packages/db/.env` must be kept in sync with root `.env.local` (both have DATABASE_URL)
